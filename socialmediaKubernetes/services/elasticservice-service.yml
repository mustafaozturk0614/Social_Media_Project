apiVersion: v1
kind: Service
metadata:
  name: elastic-cluster
spec:
  type: ClusterIP
  selector:
    app: elastic-service
  ports:
    - port: 8099
      targetPort: 8099
      name: elastic-service-port
